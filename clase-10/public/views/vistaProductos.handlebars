<div class="jumbotron">
    <h1>Productos</h1>
    {{#if allProducts}}
    <ul>
        {{#each allProducts}}
        <li>Producto Nro {{this.id}}, Titulo: {{this.title}}, Precio: {{this.price}}, Imagen: {{this.thumbnail}}</li>
        {{/each}}
    </ul>
    {{else}}
    aqui no hay para ver...
    {{/if}}
    <br>
</div>
<a href="/">Ir a carga de productos</a>